# Ansible Configuration for Medicure Continuous Deployment
[defaults]
inventory = inventory.ini
host_key_checking = False
retry_files_enabled = False
callbacks_enabled = ansible.builtin.default
result_format = yaml

[privilege_escalation]
become = False
